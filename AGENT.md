1. 项目文件目录如下：

server/
├─ api/                 # 接口处理函数
├─ config/              # 配置相关代码目录
├─ middleware/          # 中间件目录
├─ models/              # 数据模型目录
├─ routes/
│  └─ routes.go         # Gin 路由初始化和接口注册
├─ services/            # 业务服务目录
├─ utils/               # 工具函数目录
├─ main.go              # 服务启动入口，监听 :8888
├─ go.mod               # Go 模块和依赖声明
├─ go.sum               # Go 依赖校验文件
└─ AGENT.md             # 项目协作和开发规范

2. 代码实现必须简单直白，优先复用现有代码，不做无意义封装。
3. 函数需要注释说明作用，重要业务逻辑需要加注释。
4. `models/xxx模块/xxx模块.go` 只存放数据库表结构定义，例如表字段、ORM tag、表名、数据库映射关系。
5. `models/xxx模块/request/xxx模块.go` 存放接口入参结构和参数校验规则，例如 JSON 绑定、必填字段、字段类型约束。
6. `api/` 存放接口处理函数，只负责接收请求、参数绑定、调用 service、返回响应，不直接写数据库操作。
7. `services/` 存放业务处理和数据库操作相关逻辑，例如查询、新增、更新、删除、事务处理。
8. `routes/` 只负责路由注册，不写业务逻辑。
9. `middleware/` 只存放 Gin 中间件，例如鉴权、日志、跨域、异常处理。
10. `utils/` 只存放通用工具函数，不放具体业务流程。
11. 业务常量、枚举、状态码如果需要复用，统一放到 `constants/`。
12. 新增接口时，需要同步检查是否需要新增或更新：路由、api 处理函数、models 表结构、models request 入参、services 业务逻辑。
13. 修改完成后，必须对本次更改的 Go 文件执行 `gofmt`。
14. 修改完成后，必须运行 `go test ./...`，确保现有测试通过。
